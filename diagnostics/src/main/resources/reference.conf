# SPDX-License-Identifier: Apache-2.0

############################################
# Pekko Diagnostics Reference Config File  #
############################################

# This is the reference config file that contains all the default settings.
# Make your edits/overrides in your application.conf.

# Auto enable the checker
pekko.library-extensions += "org.apache.pekko.diagnostics.DiagnosticsExtension"

# Settings for the ConfigChecker, which will try to help you by finding
# potential configuration issues.
#//#config-checker
pekko.diagnostics.checker {
  # Set this to "off" to disable the ConfigurationChecker completely
  enabled = on

  # By default the checker will be run when the actor system is
  # started and it will log recommendations at warning log level.
  # Set this property to "on" if you want to fail the startup of
  # the actor system when potential configuration issues are found.
  # Then an `IllegalArgumentException` is thrown from
  # ActorSystem.apply/create if there are any issues.
  fail-on-warning = off


  # The checks are by default performed in a background thread after this
  # delay when the actor system has been started. If this property is set
  # to 0s or if fail-on-warning=on the checks will be run on the caller's
  # thread, i.e. same thread as is invoking ActorSystem.apply/create.
  # Running the checks are normally rather quick, but it can still be
  # good to run them in the background to not delay the startup.
  async-check-after = 1s

  # You can disable individual checks by adding the key of the check
  # as described in the warning messages to this list.
  disabled-checks = []

  # You can confirm that you know the meaning of a specific configuration
  # setting that is classified as an advanced "power user" setting by adding
  # the path of the configuration property to this list. When confirmed it
  # will not warn about that setting any more.
  confirmed-power-user-settings = []

  # Application specific properties that are not defined by Pekko should
  # be placed outside the "pekko" config tree, but if you still have to
  # define such a configuration property inside "pekko" you can confirm
  # that it is not a typo or misplacement by adding the configuration
  # path to this list. All properties starting with that path will not
  # be checked for typos, i.e. you can add the path of a whole section
  # here to skip everything inside that section.
  confirmed-typos = []
}

# Internal settings for the ConfigChecker. Don't change.
pekko.diagnostics.checker {

  # Definition of all properties or sections that are classified as
  # advanced "power user" settings. You should use 'confirmed-power-user-settings'
  # instead of modifying this list.
  power-user-settings = [
    # pekko-actor
    pekko.version,
    pekko.daemonic,
    pekko.actor.unstarted-push-timeout,
    "pekko.actor.router.type-mapping.*",
    # task-peeking-mode is a power user setting but changed by Play reference-overrides
    #pekko.actor.default-dispatcher.fork-join-executor.task-peeking-mode,
    pekko.actor.default-dispatcher.thread-pool-executor.task-queue-size,
    pekko.actor.default-dispatcher.thread-pool-executor.task-queue-type,
    pekko.actor.default-dispatcher.attempt-teamwork,
    pekko.actor.default-dispatcher.shutdown-timeout,
    "pekko.actor.mailbox.*",
    pekko.actor.serializers.java,
    pekko.actor.serializers.bytes,
    "pekko.scheduler.*",
    "pekko.io.*",
    pekko.diagnostics.checker.power-user-settings,
    pekko.diagnostics.checker.disabled-typo-sections,

    # pekko-remote
    pekko.remote.artery.untrusted-mode,
    pekko.remote.artery.log-received-messages,
    pekko.remote.artery.log-sent-messages,
    "pekko.remote.watch-failure-detector.*",
    pekko.remote.artery.advanced.system-message-buffer-size,
    pekko.remote.classic.resend-interval,
    pekko.remote.classic.resend-limit,
    pekko.remote.classic.initial-system-message-delivery-timeout,
    "pekko.remote.adapters.*",
    pekko.remote.classic.netty.tcp.transport-class,
    pekko.remote.classic.netty.tcp.applied-adapters,
    pekko.remote.classic.netty.tcp.enable-ssl,
    pekko.remote.classic.netty.tcp.use-dispatcher-for-io,
    pekko.remote.classic.netty.tcp.write-buffer-high-water-mark,
    pekko.remote.classic.netty.tcp.write-buffer-low-water-mark,
    pekko.remote.classic.netty.tcp.send-buffer-size,
    pekko.remote.classic.netty.tcp.receive-buffer-size,
    pekko.remote.classic.netty.tcp.backlog,
    pekko.remote.classic.netty.tcp.tcp-nodelay,
    pekko.remote.classic.netty.tcp.tcp-keepalive,
    pekko.remote.classic.netty.tcp.tcp-reuse-addr,
    "pekko.remote.classic.netty.tcp.server-socket-worker-pool.*",
    "pekko.remote.classic.netty.tcp.client-socket-worker-pool.*",
    "pekko.remote.classic.netty.udp.*",
    pekko.remote.classic.netty.ssl.enable-ssl,
    pekko.remote.classic.netty.ssl.enable-ssl,
    pekko.remote.classic.netty.ssl.transport-class,
    pekko.remote.classic.netty.ssl.applied-adapters,
    pekko.remote.classic.netty.ssl.enable-ssl,
    pekko.remote.classic.netty.ssl.use-dispatcher-for-io,
    pekko.remote.classic.netty.ssl.write-buffer-high-water-mark,
    pekko.remote.classic.netty.ssl.write-buffer-low-water-mark,
    pekko.remote.classic.netty.ssl.send-buffer-size,
    pekko.remote.classic.netty.ssl.receive-buffer-size,
    pekko.remote.classic.netty.ssl.backlog,
    pekko.remote.classic.netty.ssl.tcp-nodelay,
    pekko.remote.classic.netty.ssl.tcp-keepalive,
    pekko.remote.classic.netty.ssl.tcp-reuse-addr,
    "pekko.remote.classic.netty.ssl.server-socket-worker-pool.*",
    "pekko.remote.classic.netty.ssl.client-socket-worker-pool.*",
    "pekko.remote.gremlin.*",
    "pekko.remote.backoff-remote-dispatcher.*"
    pekko.actor.serializers.pekko-containers,
    pekko.actor.serializers.proto,
    pekko.actor.serializers.daemon-create,
    pekko.actor.serializers.artery,
    pekko.actor.serializers.pekko-misc,
    pekko.actor.serializers.pekko-system-msg,

    # pekko-cluster
    pekko.cluster.seed-node-timeout,
    pekko.cluster.retry-unsuccessful-join-after,
    pekko.cluster.periodic-tasks-initial-delay,
    pekko.cluster.gossip-interval,
    pekko.cluster.gossip-time-to-live,
    pekko.cluster.leader-actions-interval,
    pekko.cluster.unreachable-nodes-reaper-interval,
    pekko.cluster.publish-stats-interval,
    pekko.cluster.gossip-different-view-probability,
    pekko.cluster.reduce-gossip-different-view-probability,
    "pekko.cluster.scheduler.*",
    "pekko.cluster.failure-detector.*",
    pekko.actor.serializers.pekko-cluster,

    # pekko-cluster-metrics
    pekko.cluster.metrics.supervisor,

    # pekko-cluster-tools
    pekko.cluster.pub-sub.name,
    pekko.cluster.pub-sub.gossip-interval,
    pekko.cluster.pub-sub.removed-time-to-live,
    pekko.cluster.client.receptionist.name,
    pekko.cluster.singleton.hand-over-retry-interval,
    pekko.cluster.singleton.min-number-of-hand-over-retries,
    pekko.cluster.singleton-proxy.singleton-name,
    pekko.cluster.singleton-proxy.singleton-identification-interval,

    # pekko-cluster-sharding
    pekko.cluster.sharding.guardian-name,
    pekko.cluster.sharding.retry-interval,
    pekko.cluster.sharding.shard-failure-backoff,
    pekko.cluster.sharding.entity-restart-backoff,
    pekko.cluster.sharding.rebalance-interval,
    "pekko.cluster.sharding.coordinator-singleton.*",

    # pekko-persistence
    pekko.persistence.no-snapshot-store.class,
    "pekko.persistence.dispatchers.*",
    "pekko.persistence.journal-plugin-fallback.*",
    "pekko.persistence.snapshot-store-plugin-fallback.*"
    "pekko.persistence.journal.inmem.*",
    pekko.persistence.snapshot-store.local.class,
    pekko.persistence.snapshot-store.local.plugin-dispatcher,
    pekko.persistence.journal.leveldb.class,
    pekko.persistence.journal.leveldb.plugin-dispatcher,
    pekko.persistence.journal.leveldb.fsync
  ]

  # Definition of sections or properties that are not checked
  # for typos and misplacements, because they are supposed to
  # be used. You should use 'confirmed-typos' instead of modifying
  # this list.
  disabled-typo-sections = [
    "pekko.actor.serializers",
    "pekko.actor.serialization-bindings",
    "pekko.actor.serialization-identifiers",
    "pekko.actor.mailbox.requirements",
    "pekko.persistence.publish-plugin-commands",
    "pekko.coordinated-shutdown.phases",
    "pekko.coordinated-shutdown.reason-overrides",
    "pekko.cluster.role",
    # kryo mappings are classnames of what one wants to use kryo for, so they won't be classes not known to us, but good.
    "pekko.actor.kryo.mappings",
    # Artery is still experimental, so let's not warn about typos here yet
    "pekko.remote.artery",
    # These are special, empty-by-default settings in Pekko HTTP
    "pekko.http.server.parsing", # empty by default, but used to override values from pekko.http.parsing
    "pekko.http.client.parsing", # empty by default, but used to override values from pekko.http.parsing
    "pekko.http.host-connection-pool.parsing", # empty by default, but used to override values from pekko.http.parsing
    "pekko.http.host-connection-pool.client" # empty by default, but used to override values from pekko.http.client,
    "pekko.persistence.multi-data-center.cross-reading-replication.cassandra-journal",
    # pekko-discovery
    "pekko.discovery.config.services",
    # pekko-management
    "pekko.management.http.routes",
    "pekko.management.health-checks.startup-checks",
    "pekko.management.health-checks.readiness-checks",
    "pekko.management.health-checks.liveness-checks",
    # Not covered by config checking
    "pekko.kafka",
    # Even if we add pekko-projection-slick and jackson serializer, most are dynamic so easier to add it here
    "pekko.projection.slick",
    "pekko.serialization.jackson",
    # no values in reference.conf
    "pekko.persistence.r2dbc.state.additional-columns",
    "pekko.persistence.r2dbc.state.custom-table",
    "pekko.persistence.r2dbc.state.change-handler",
    "pekko.persistence.testkit.snapshotstore.pluginid",
    "pekko.persistence.testkit.journal",
  ]
}
#//#config-checker

#//#starvation-detector
# The starvation detector is a dedicated thread that schedules a task on the system's
# main dispatcher and checks if the execution time is less than the threshold. Exceeding the
# threshold is an indication for thread starvation. The starvation detector will log a warning
# in that case with statistics and stack traces of the dispatcher's threads.
pekko.diagnostics.starvation-detector {
  # The interval to check task execution time on the dispatcher.
  #
  # Set to 0 or "off" to disable checking.
  check-interval = 1s

  # The starvation detector only runs after the initial delay. The idea is that during startup,
  # initial tasks like class loading, warming up JIT and similar environment issues can decrease
  # the overall throughput. As this is usually a transient condition, checking will only run after
  # the configured time.
  #
  # Set to 0 or "off" to run checking right from the start.
  initial-delay = 10s

  # The maximum time during which the dispatcher is expected to execute the starvation detection task.
  # If the dispatcher takes longer to execute the task, the dispatcher (and CPU or other infrastructure
  # that is needed to run tasks) is considered busy and a warning is logged.
  #
  # The default value was chosen to be high enough to decrease the likelihood of false positives while
  # still being likely to show severe problems early.
  #
  # For many applications (and also for internal Pekko messaging) much smaller delays can already become
  # a problem. To detect smaller dispatcher related delays, decrease the value, but keep in mind the
  # higher chance of false positives.
  max-delay-warning-threshold = 100 ms

  # The minimum time between consecutive warnings.
  #
  # When thread starvation is detected it is likely that it will last for a longer period of time.
  # This setting can be used to prevent that warnings from the starvation detector flood the logs.
  warning-interval = 10 seconds

  # When the starvation detector triggers, it will show aggregated information about the state of threads in a pool.
  # Threads are aggregated by traces and, by default, the top 5 stack traces (by number of threads having the same trace)
  # are shown.
  #
  # Use "infinite" to show aggregated numbers for all threads.
  thread-traces-limit = 5
}
#//#starvation-detector
